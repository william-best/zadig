apiVersion: v1
kind: Service
metadata:
  labels:
    app: vote
  name: vote
spec:
  type: NodePort
  ports:
  - name: "vote-service"
    port: 5000
    targetPort: 80
  selector:
    app: vote
    version: rc-origin
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: vote
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: vote-e2e-10x-$Namespace$.c79f7013d94ef4a0b826101ee6b099d55.cn-huhehaote.alicontainer.com
    http:
      paths:
      - path: /
        backend:
          serviceName: vote
          servicePort: 5000
